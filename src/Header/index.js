import React from "react";
import logo from '../logo.png';
import "./styles.css";

const Header = () => {
  return <header>
            <img src={logo}/>
          </header>;
};

export default Header;
